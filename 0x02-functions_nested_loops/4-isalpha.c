#include "main.h"

/**
 * _isalpha - check alphabetic character
 * return: always 0
 */
int _isalpha(int c);
{
	if ((c = _isalpha('h')) || (c = _isalpha('H')))
	{
		return (1);
	}
	else if
	{
		return (0);
	}
}
